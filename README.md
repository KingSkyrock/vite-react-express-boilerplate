# React + Vite + Express + SCSS

This is a boilerplate.

## Build and run:

### To run locally:

Run in development environment:
```sh
npm install
npm run dev
```

Run in production environment:
```sh
npm install
npm run build
npm run preview
```